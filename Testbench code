`timescale 1ns/1ps

module tb_counter4;

reg clk;
reg rst;
wire [3:0] q;

counter4 uut(
    .clk(clk),
    .rst(rst),
    .q(q)
);

always #5 clk = ~clk;

initial
begin
    clk = 0;
    rst = 1;

    #20 rst = 0;

    #200 $finish;
end

endmodule
